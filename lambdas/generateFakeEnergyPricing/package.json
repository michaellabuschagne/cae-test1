{
  "name": "generatefakeenergypricing",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -e 'require(\"./src/handler.js\").generateFakeEnergyData()'",
    "clean": "rimraf dist *.zip",
    "copy": "babel src package.json package-lock.json --out-dir dist --copy-files",
    "prod": "cross-env NODE_ENV=production npm install --prefix dist",
    "zip": "zip -rXFS9 generateFakeEnergyPricing-v1.zip dist",
    "build": "npm run clean && npm run copy && npm run prod && npm run zip",
    "s3upload": "aws s3 cp generateFakeEnergyPricing-v1.zip s3://cae-test1-deployment-artifacts-eu-west-1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.11.6",
    "@babel/core": "^7.11.6",
    "aws-sdk": "^2.755.0",
    "cross-env": "^7.0.2",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@babel/runtime-corejs3": "^7.11.2"
  },
  "description": ""
}
