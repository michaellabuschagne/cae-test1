{
  "name": "generateFakeEnergyPricing",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node -e 'require(\"./src/handler.js\").generateFakeEnergyData()'",
    "build": "mkdir -p ./build &&  && cross-env NODE_ENV=production npm install --prefix dist && zip -rXFS9 function.zip dist",
    "push-artifacts": "aws s3 cp ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "aws-sdk": "^2.755.0"
  }
}
